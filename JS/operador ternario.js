let num = 3;

(num%2==0) ? console.log(`${num} es par`) : console.log(`${num} no  es par`);


(num%2==0) ? 
(
    console.log(`${num} es par`), 
    console.log(`${num} es par 2`)
)
:
(
    console.log(`${num} no es par`), 
    console.log(`${num} no es par 2`)
)

